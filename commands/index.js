const start = require('./start');
const check = require('./check');
const { support } = require('./support');
const { supportScene } = require('./support');
const { buyScene } = require('./buy');
const { buy } = require('./buy');
const description = require('./description');

module.exports = {
  start,
  check,
  support,
  supportScene,
  buy,
  buyScene,
  description,
};
